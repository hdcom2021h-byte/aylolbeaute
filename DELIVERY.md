# تسليم موقع AYLOL BEAUTY

## التشغيل والنشر

الموقع جاهز كملفات موقع ثابت، ولم يُنشر على استضافة أو نطاق ضمن هذا التسليم.
ارفعي `index.html` و`headr.html` و`footer.html` ومجلد `media` معًا إلى المجلد الرئيسي للاستضافة.
حافظي على أسماء الملفات والمجلدات وحالة الأحرف؛ لا تغيّري اسم `headr.html`.

للمعاينة على جهاز يحتوي Python، افتحي مجلد الموقع في الطرفية وشغّلي:

```powershell
python -m http.server 8000 --bind 127.0.0.1
```

ثم افتحي `http://127.0.0.1:8000/index.html`.
لا تفتحي الملف بالنقر المزدوج عبر `file://` لأن تحميل الهيدر والفوتر يحتاج HTTP.
لا يحتاج الموقع إلى Python على الاستضافة؛ يُستخدم الأمر للمعاينة وتحديث قائمة المعرض فقط.

## وظائف الموقع وحدودها

- الحجز لخدمتي Makeup Artist وHair Style فقط. يعرض ملخصًا ثم يفتح واتساب برسالة جاهزة للرقم 966568178738؛ تُرسل الزائرة الرسالة بنفسها. لا توجد قاعدة بيانات أو إدارة تلقائية لتوفر المواعيد.
- التقييم بالنجوم والملاحظة دون أسماء محفوظان على متصفح الزائرة فقط. لا يصل التقييم إلى الإدارة ولا يُنشر للجميع. يتطلب ذلك تطوير خدمة تخزين مشتركة لاحقًا.
- رابط «حجوزات» يفتح قسم «احجزي إطلالتك»، وروابط الهيدر تتوسط الأقسام القصيرة وتُظهر بداية محتوى الأقسام الطويلة تحت الهيدر الثابت.
- ساعات العمل المعروضة: 9 صباحًا إلى 10 مساءً. العنوان: خميس مشيط / المحالة.
- عرض اليوم الوطني 96 مُدخل بمدة 10–30 سبتمبر؛ لا يُخفى تلقائيًا عند انتهاء الفترة. عدّليه أو أزيليه بعد انتهاء العرض.
- الصور والخطوط والفيديوهات المستخدمة في الصفحة محفوظة محليًا. فتح حسابات التواصل والخرائط وواتساب يحتاج اتصالًا بالإنترنت.

## تحديث المحتوى

- النصوص والأسعار والعرض: `index.html`.
- روابط القائمة والشعار: `headr.html`، وروابط أسفل الموقع: `footer.html`.
- فيديو الرئيسية: استبدلي `media/hero/hero.mp4` بنفس الاسم.
- صور الخدمات: داخل `media/services`؛ أسماء الملفات المستخدمة ظاهرة في `index.html`.
- صورة التعريف المحسّنة: `media/about/salon-enhanced.png`، والشعار: `media/logo-gold.png`.
- المعرض: أضيفي الصور أو الفيديو إلى `media/gallery/Makeup` أو `Hair` أو `Brides` أو `Nails` ثم شغّلي:

```powershell
python media/gallery/update-gallery.py
```

ملف `media/gallery/photo-settings.json` يحفظ ترتيب الصور وعناوينها وسطوع عرضها.
ارفعي الوسائط الجديدة وملف `gallery.js` المحدّث، ثم حدّثي الصفحة بـ Ctrl + F5.

## التحقق المنفذ

اختُبر الموقع في متصفح Chrome محلي بمقاسات 1440×900 و820×1180 و390×844:
التنقل عبر روابط الهيدر، عدم وجود تمرير أفقي، طي الأسعار والبحث، فلترة المعرض،
فتح الصور والفيديو والعرض وإغلاقها، حفظ التقييم محليًا، ومعاينة الحجز دون إرسال رسالة.
لم تظهر أخطاء JavaScript أو استجابات HTTP فاشلة أثناء هذه الاختبارات.
لم يُختبر على أجهزة iPhone فعلية أو Safari، ولم يُختبر نشره على استضافة العميلة.

نسخة التسليم تستبعد ملفات الاختبار والملفات المؤقتة ونسخة الفيديو الاحتياطية.
