# مراجعة AYLOL BEAUTY قبل النشر

## النتيجة

اجتازت النسخة الحالية الاختبارات المحلية كموقع Static عبر HTTP، في جذر الموقع
وكذلك تحت مسار فرعي `/preview/` لمحاكاة بنية مواقع مشاريع GitHub Pages.
لا يعتمد تشغيل الموقع على خادم تطبيقات أو قاعدة بيانات أو خطوة بناء.
لم يُنفّذ رفع إلى GitHub أو نشر على Render، ولم تُنفّذ أوامر commit أو push.

## التعديلات في هذه المراجعة فقط

| الملف | الإصلاح |
| --- | --- |
| `headr.html` | إعادة تركيز لوحة المفاتيح إلى زر القائمة عند إغلاق قائمة الجوال، بدل بقائه داخل القائمة المخفية. |
| `index.html` | نقل التركيز إلى نافذة مراجعة الحجز عند فتحها، وحصر التنقل بـ Tab داخلها، وإعادته إلى زر المراجعة عند الإغلاق. منع تنفيذ إغلاق نافذة الحجز عندما تكون مغلقة أصلًا. إزالة `src` الفارغ من صورة نافذة التكبير، وإزالة الخاصية عند الإغلاق بدل تعيين مسار فارغ. |
| `tmp/delivery-check/static_audit.py` | إضافة أداة فحص محلية لبنية الملفات، والمسارات وحالة الأحرف، والتجاوب، ولوحة المفاتيح، والتشغيل تحت مسار فرعي. هذا ملف اختبار وليس جزءًا مطلوبًا لتشغيل الموقع. |
| `STATIC_REVIEW.md` | هذا التقرير. |

التعديلات الموجودة في المشروع قبل بداية هذه المراجعة محفوظة كما هي.
لم تتغير الألوان أو الخطوط أو تصميم البطاقات، ولم تُحذف صور أو فيديوهات أو أقسام.

## الفحوص المنفذة

- تحليل `index.html` و`headr.html` و`footer.html` دون أخطاء بنيوية في HTML.
- تحليل CSS داخل الصفحات وجميع ملفات CSS المحلية، وفحص قواعدها وتصريحاتها.
- التحقق من صياغة JavaScript المضمنة وجميع ملفات JavaScript داخل `media`.
- تحميل الصفحة الرئيسية والهيدر والفوتر والوسائط محليًا دون أخطاء JavaScript أو استجابات HTTP فاشلة أثناء الاختبارات.
- التحقق من جميع أهداف الروابط الداخلية وعدم تكرار المعرّفات `id`.
- اختبار روابط الهيدر والفوتر والتنقل، وفتح الأقسام مباشرة من رابط يحتوي `#`.
- مطابقة مسارات الصور والفيديو والخطوط وCSS وJavaScript للملفات الفعلية، بما في ذلك حالة الأحرف اللازمة على استضافة Linux.
- التأكد من عدم وجود مسار جهاز مثل `C:\Users...` أو `file://` في مسارات وسائط الصفحة أو سكربتات الموقع.
- التحقق من تحميل الصور، ومعرضه ذي العناصر الثمانية، وفك ترميز فيديو الشعر داخل نافذة العرض.
- اختبار طي الأسعار والبحث، وفلترة المعرض، ونوافذ الصورة والفيديو والعرض، والتقييم المجهول، ومعاينة الحجز.
- اختبار مقاسات 320×740 و390×844 و768×1024 و820×1180 و1081×900 و1440×900 و1920×1080، دون تمرير أفقي أو تغطية الهيدر لبداية المحتوى المستهدف.
- فحص لوحة المفاتيح بعد إصلاح قائمة الجوال ونافذة الحجز.

## حدود التحقق والنشر

- الاختبارات بمتصفح Chrome محلي مع تغيير مقاسات الشاشة، وليست على جميع الأجهزة الفعلية أو جميع المتصفحات.
- روابط واتساب والاتصال والخرائط والحسابات الخارجية مرتبطة بالعناوين المدخلة في المشروع. لم يُرسل أي حجز أو رسالة ولم يُجرَ اتصال؛ توفر هذه الخدمات الخارجية ليس مضمونًا بفحص الملفات المحلية.
- الموقع مناسب للاستضافة الثابتة على GitHub Pages أو Render Static Site عند نشر ملفات الموقع مع مجلد `media` والحفاظ على هيكلها النسبي. لم تُختبر استضافة فعلية ضمن هذه المراجعة.
- افتحي الموقع عبر HTTP/HTTPS؛ فتح `index.html` مباشرة عبر `file://` يمنع بعض المتصفحات من جلب أجزاء الهيدر والفوتر.
- التقييمات تبقى محلية في متصفح الزائرة، والحجز يجهّز رسالة واتساب؛ لا توجد إدارة مواعيد أو تقييمات مشتركة من الخادم.
- نتائج الفحص المفصلة محفوظة في `tmp/delivery-check/static-audit-results.json`. ملفات `tmp` ونسخة ZIP السابقة ليست مطلوبة لتشغيل الموقع، ولم يتم حذفها أو تعديل أرشيف التسليم السابق.
